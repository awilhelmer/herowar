<div class="sidebar-panel-headline">Environment</div>
<div class="sidebar-panel-content">
	<div class="tree">
		{{#unless id}}
			<div class="loading"></div>
		{{else}}
			{{#each children}}
				{{name}}
			{{/each}}
		{{/unless}}
	</div>
</div>